<Window x:Class="QuestResume.Desktop.MasterKeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuestResume - Senha Mestre" Height="240" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Índice criptografado" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Senha mestre" Margin="0,0,0,3"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,20,0,10" Padding="5"/>

        <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Entrar" Click="OnUnlockClick" Padding="15,5" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
